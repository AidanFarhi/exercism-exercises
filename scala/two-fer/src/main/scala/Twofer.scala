object Twofer {
  def twofer(name: String): String = {
    // val nullResponse: String = "One for you, one for me."
    // val nonNullResponse: String = s"One for $name, one for me"
    // if (name == Nil) {
    //   return nullResponse
    // }
    // return nonNullResponse
    return ""
  }
}
